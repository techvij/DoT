psycopg2-binary==2.9.9
pandas==2.2.2
PyYAML>=6.0.2,<7
click>=8.1.8,<9
python-dotenv==1.0.1
requests>=2.31.0,<3

# BigQuery (optional — only needed when type: bigquery)
google-cloud-bigquery==3.25.0
pyarrow==17.0.0
db-dtypes==1.3.0

# Report layer (optional — only needed when report.claude.enabled or report.slack.enabled)
anthropic>=0.34.0,<1
